{{> header}}

<h2>{{title}}</h2>

<article>
    {{{contents}}}
</article>

{{#each entries}}

	<h1>{{content_title}}</h1>
	<h2>{{content_name}}</h2>
	{{{details}}}
	<img src="{{image.url}}">

{{/each}}

{{> footer}}