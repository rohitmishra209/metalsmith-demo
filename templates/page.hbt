{{> header}}

<h2>{{title}}</h2>

<article>
    {{{contents}}}
</article>
<h1>{{rohit}}</h1>

{{#each entries}}

	<h1>{{title}}</h1>
	<h2>{{first_name}}</h2>
	<h3>{{uid}}</h2>
	<p>{{details}}</p>
	 <img src="{{banner_image.url}}">

{{/each}}

{{> footer}}